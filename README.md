# BMI API — FastAPI Project

This is a simple BMI (Body Mass Index) calculator built using FastAPI. It takes weight (in pounds) and height (in inches) as query parameters and returns the calculated BMI.

## 🚀 How to Run

```bash
uvicorn bmi_api.main:app --reload
